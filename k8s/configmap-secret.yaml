apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: expense-tracker
data:
  SPRING_DATA_MONGODB_DATABASE: "expensedb"

---

# base64 values shown below are for "admin" / "admin" / "expensedb" — change them for real use:
#   echo -n "yourvalue" | base64
apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
  namespace: expense-tracker
type: Opaque
data:
  MONGO_USERNAME: YWRtaW4=
  MONGO_PASSWORD: YWRtaW4=
  MONGO_DATABASE: ZXhwZW5zZWRi
